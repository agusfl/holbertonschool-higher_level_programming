#!/usr/bin/node
// Update this script to replace the value 12 with 89

const myObject = {
  type: 'object',
  value: 12
};
console.log(myObject);
myObject.value = 89; // cambiamos un elemento del objeto myObject por un nuevo valor
console.log(myObject);
